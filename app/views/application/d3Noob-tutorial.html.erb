<h1>D3 tutorial by D3Noob.org</h1>
<h3>svg input tab</h3>

<%= render 'housesvg' %>

<!-- Slider HTML -->
<!-- <div id="vertical-slider" style="height: 200px;"></div> -->

<!-- Label input HTML -->
<p>
  <label for="nValue" 
         style="display: inline-block; width: 240px; text-align: right">
         angle = <span id="nValue-value"></span>
  </label>
  <input type="number" min="-720" max="720" step="5" value="0" id="nValue" >
</p>
<p>
  <label for="nRatio" 
         style="display: inline-block; width: 240px; text-align: right">
         ratio = <span id="nRatio-value"></span>
  </label>
  <input type="number" min="-10" max="10" step=".10" value="0" id="nRatio" >
</p>

<!-- <svg width="500" height="500">
  <polyline fill="green" stroke="grey" stroke-width="2"
      points="200,275
            225,300
            225,325
            175,325
            175,300
            200,275"
  /> -->

<p> words before the script</p>


</svg>

<script>

var width = 600;
var height = 300;
var nRatio = 1;

var holder = d3.select("body")
      .append("svg")
      .attr("width", width)    
      .attr("height", height); 

// // draw the element
// holder.append("text")
//   .style("fill", "black")
//   .style("font-size", "56px")
//   .attr("dy", ".35em")
//   .attr("text-anchor", "middle")
//   .attr("transform", "translate(300,150) rotate(0)")
//   .text("d3noob.org");

// // when the input range changes update value 
// d3.select("#nValue").on("input", function() {
//   update(+this.value);
// });

// // Initial update value 
// update(0);

// // adjust the text
// function update(nValue) {

//   // adjust the value
//   holder.select("text") 
//     .attr("transform", "translate(300,150) rotate("+nValue+")");
// }

// alternate append
// draw the element
holder.append("rect")
  .style("fill", "black")
  .style("stroke", "black")
  .style("stroke-width", "10px")
  .attr("x1", 0)
  .attr("y1", 0)
  .attr("width", "175")
  .attr("height","175")
  .attr("rect-anchor", "middle")
  .attr("transform", "translate(225,75) rotate(0)")
  ;

// alternate append: polygon
holder.append("polyline")
  .style("fill", "green")
  .style("stroke", "black")
  .style("stroke-width", "2px")
  .attr("points", "175,175, 200,200, 200,225, 150,225, 150,200, 175,175")
  .attr("transform", "scale(1.5,1.5) transform(100,100)")
  ;



// when the input range changes update value 

//rotate/angle update
d3.select("#nValue").on("input", function() {
  update1(+this.value);
});

//ratio/scale update
d3.select("#nRatio").on("input", function() {
  update2(+this.value);
});
// Initial update value 
update1(0);
update2(0);


// adjust the form
function update1(nValue) {
  // adjust the value
  holder.select("rect") 
    .attr("transform", "translate(275,100) rotate("+nValue+", 75,75)");

}

// function update(nRatio) {
function update2(nRatio) {

  // adjust the ratio
  holder.select("rect") 
    .attr("width", "200" * (1 + nRatio))
    .attr("height","200" * (1 + nRatio))

//   // adjust the ratio
  holder.select("polyine") 
    .attr("transform", "translate(0,0) scale(1 + "+nRatio+") translate(175,175)");
}





</script>

  <p> words after the script</p>
